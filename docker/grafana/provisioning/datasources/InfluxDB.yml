#
# Copyright 2013-2025 Software Radio Systems Limited
#
# By using this file, you agree to the terms and conditions set
# forth in the LICENSE file which can be found at the top level of
# the distribution.
#

apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: ${INFLUXDB3_EXTERNAL_URL}
    uid: JOSE3g9KVz
    secureJsonData:
      token: ${INFLUXDB3_AUTH_TOKEN}
    jsonData:
      version: SQL
      dbName: ${INFLUXDB3_BUCKET}
      insecureGrpc: true
      maxSeries: 100000
